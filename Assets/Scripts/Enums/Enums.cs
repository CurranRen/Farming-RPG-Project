// 库存位置枚举
public enum InventoryLocation
{
    player,
    chest,
    count
}

// 工具效果枚举
public enum ToolEffect
{
    none,
    watering
}

// 玩家面朝方向枚举
public enum Direction
{
    up,
    down,
    left,
    right,
    none
}

// 物品类型枚举
public enum ItemType
{
    Seed,
    Commodity,
    Watering_tool,
    Hoeing_tool,
    Chopping_tool,
    Breaking_tool,
    Reaping_tool,
    Collecting_tool,
    Reapable_scenary,
    Furniture,
    none,
    count
}